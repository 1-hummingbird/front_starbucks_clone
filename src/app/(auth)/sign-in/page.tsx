import SignInForm from '@/components/form/SignInForm';

const Page = () => {
  return (
    <main className="flex max-h-screen w-full flex-col justify-between bg-white">
      <SignInForm />
    </main>
  );
};

export default Page;
